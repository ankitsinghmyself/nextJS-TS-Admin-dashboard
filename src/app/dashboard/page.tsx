"use Client";

import Sidebar from "@/components/Sidebar";

// `app/dashboard/page.tsx` is the UI for the `/dashboard` URL

export default function Page() {
  return (
    <>
      <Sidebar>Hello, Dashboard Page!</Sidebar>
    </>
  );
}
